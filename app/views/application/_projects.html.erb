<% if @projects.empty? %>
  No projects
<% else %>
  <ul class="projects" rel="projects" style="width: <%= @projects.count * 160 %>px;">
  <% @projects.each do |project| %>
    <li class="project">
        <a class="project" href="assets/project/<%= project.title.downcase.gsub(/\s/, '') %>/full.png" title="<%= project.description %>">
        <span><%= project.title %></span>
        <div class="arrow"></div>
        <%= image_tag "/assets/project/#{project.title.downcase.gsub(/\s/, '')}/thumb.png" %>
        </a>
    </li>
    </a>
  <% end %>
  </ul>
<% end %>